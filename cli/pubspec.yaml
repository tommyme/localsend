name: localsend_cli
description: LocalSend CLI - Send and receive files over LAN from the command line.
version: 1.0.0
publish_to: "none"

environment:
  sdk: ^3.5.0

dependencies:
  args: 2.4.2
  common:
    path: ../common
  mime: ^1.0.0
  path: 1.9.0
  http: ^1.2.0

dev_dependencies:
  lints: ^2.0.0
